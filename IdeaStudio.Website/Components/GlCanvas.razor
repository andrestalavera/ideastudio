@* GlCanvas — the single WebGL2 canvas host. Mounted once by MainLayout.
   Engine boot is triggered by the orchestrator in wwwroot/src/cinema/index.js
   via JS interop. This component is presentational: just the element. *@

<canvas id="gl-canvas" aria-hidden="true"></canvas>
