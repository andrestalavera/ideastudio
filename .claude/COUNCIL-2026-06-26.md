# Council Verdict — IdeaStudio full-site improvement (2026-06-26)

Facilitated council of 8 gating seats audited the live codebase (build GREEN).

**Verdict: REVISE & PROCEED** — no production-blocking P0s, but one binding code
BLOCK (Viktor: `async void` ban violation) and several P1 security/UX/SEO leaks.
Binding items cleared in Wave 1. Risky/measurement items (CSP nonce, AOT/ICU
trim, render-mode/prerender) deferred and noted — they need a spike, not a
drive-by edit.

## Cross-seat consensus (deduped)

### Wave 1 — high value, low risk, disjoint files
- **A · Frontend a11y/perf/UX** (Théo, Aiko, Yuki)
  - LCP reveal-gate: `[data-reveal]` visible by default, hidden only once JS adds
    `cinema-ready` to `<html>` (Théo P0).
  - Debounce the body MutationObserver in `reveals.js` (Théo P2).
  - Masthead: add **Contact** to nav (Aiko/Sophie P1); remove `role="menubar"/
    "menuitem"` (Yuki P1); backdrop not focusable + focus mgmt (Aiko/Yuki P1).
  - CultureSelector backdrop not focusable (Yuki P1).
  - Hero: one primary CTA, demote others (Aiko P1, DESIGN.md §5).
  - Contact: focus first invalid field + `aria-live` error summary (Aiko/Yuki P1).
  - Tap targets ≥44px on mobile controls (Aiko P2).
- **B · SEO/content/assets** (Sophie, Omar, Théo)
  - Static OG/Twitter + canonical baseline in `index.html` (Sophie P1).
  - Avatar preload (Théo P2).
  - `Services.razor` distinct, keyword-rich FR/EN title (Sophie P1).
  - Pricing coherence across Home / JSON-LD / service FAQs (Sophie P2).
  - Delete dead heavy assets + static resume PDFs (Théo/Omar P1–P2).
- **C · Code quality** (Viktor, Elena)
  - `LocalizedComponent` `async void` → `InvokeAsync` (BINDING, Viktor/Elena P1).
  - `LegacyRedirect` hard-coded `/fr/...` → `ILocalizedRoute` (Viktor P1).
  - `SlugTranslator` pair by stable key, route via gateway (Viktor/Elena P1).
  - Dedup `ExtractCulture` (Elena quick win).
  - Narrow swallow-all catch in `LazyLoadingService` (Elena P1, conservative).
- **D · Security/deps** (Ravi)
  - `npm audit fix` + nodemailer bump in `netlify/functions` (Ravi quick win).
  - `BlogArticle` Markdig: disable raw HTML / sanitize (Ravi P1).
  - `contact.mts` CRLF-strip replyTo; rate-limit config in `netlify.toml` (Ravi P1).
  - Cache-Control `immutable` for `/js/*` (Omar P1).

### Wave 2 — tests follow the code
- IntegrationTests: assert new routes (Contact/About/Blog/Faq/CaseStudy/…) (Yuki P1).
- Contact validation unit tests; SlugTranslator round-trip tests (Yuki/Viktor P1).

### Deferred (need a spike — NOT drive-by)
- CSP `'unsafe-inline'` removal via nonces (Ravi P1) — static-host nonce strategy.
- Drop AOT / trim ICU to fr+en shards (Théo P1) — measure published size both ways.
- Prerender/SSG for crawler-visible metadata (Théo/Sophie P1) — render-mode change.
- resume-pdf pack pinned by SHA + Host allowlist (Ravi P2).
- Remove endpoint-less `IdeaStudio.Apis` from solution (Elena P2) — owner decision.

Constraint: **no commits**. Verify each wave with `dotnet build` + `dotnet test`.

---

## Outcome (closed)

- **Wave 1 + 2: DONE.** Build green, tests **90 → 143**, cinema bundle 21.5 KB
  (under budget), ~6.9 MB dead assets removed.
- **Acceptance verification: APPROVE** — Viktor (6/6 correctness points, 0 defects)
  and Yuki (5/5 a11y points, only P2 nice-to-haves). One P2 (Contact error-summary
  `data-reveal`) folded in afterward.
- Append to MEMORY.md index: see `full-site-improvement-loop` memory.

---

## Extended session (same day) — deferred items + new directives

All green throughout; tests **90 → 159**; no commits.

**Deferred items, now DONE:**
- **Prerender** — per-route static metadata shells (22 files, FR/EN) with title/desc/
  canonical/hreflang/OG/JSON-LD baked, served before the SPA fallback (`scripts/
  prerender-shells.mjs`, wired via package.json). (Full-DOM render still via existing
  puppeteer `scripts/prerender.mjs`.)
- **CSP** — dropped `'unsafe-inline'` from script-src via **sha256 hashes** of the
  inline bootstrap scripts (static-host compatible); externalized the inline error-UI
  handler to `js/error-ui.js`. Tooling: `scripts/csp-hashes.mjs`.
- **resume-pdf** — Chromium pack SHA-256 verified (fail-closed) + Host-header SSRF
  allowlist + rate limit + hardened response headers.
- **Architecture** — ConcurrentDictionary→Dictionary; collapsed the duplicate i18n
  JSON-loading stack into `ILazyLoadingService`; removed endpoint-less `IdeaStudio.Apis`
  from the solution.

**New owner directives (more marketing / simpler / less technical except CV):**
- Brief: `.claude/BRIEF-positioning.md`. Audience shifted to the buyer.
- **Home** simplified 9→7 sections, one primary + one secondary CTA, de-jargoned,
  duplicate CtaBand removed, lead trimmed for mobile fold.
- **Services / Realisations / Trainings / Blog / FAQ / About** rewritten to buyer-
  outcome language; jargon stripped; CTA labels unified (FR "Réserver un appel" / EN
  "Book a call"); service-page hero CTAs added; About decoupled from the technical CV.
- **CV/Resume** deepened to 100% technical (the single home for engineering depth).
- JSON content reorganised (outcome-first, plain stat labels, de-jargoned chips).

**New feature:** home-made observable **state store** (`State/` — Store/AppState/
AppReducer/actions + StoreSubscriber), chosen over Fluxor for AOT/bundle/fit; theme/
consent/culture now flow through it behind unchanged service APIs; 16 new tests.

**Acceptance:** Viktor APPROVE (content-wave regressions + store/service refactor),
Sophie ON-BRIEF after polish, Aiko CONCERN→addressed, Yuki/Ravi earlier APPROVE.

## Luxe elevation pass (same day) — "make the site more luxe" + readability/a11y

Design council: Aiko (spec + soft-veto), Théo (perf-safe), Stanislas (premium brand).
Thesis: **luxe = restraint + craft, not decoration.** Tests **159 → 167**; green; no commits.

- **Tokens (foundation):** radius scale (sm→xl/pill), `$ease-lux` decel easing +
  slower hover tier (~260ms), dark elevation tokens with top inner-light line
  (`--elev-1/2`), `--tint-duck`, `bg-raised` nudged lighter, font-feature-settings +
  optical-sizing, tighter display tracking + 46ch lead measure, layered focus ring,
  softened `::selection`. DESIGN.md §8 synced.
- **Gradient made RARE** (Aiko's soft-veto): masthead pill demoted from permanent
  gradient → hairline + elevation; gradient reserved to footer hairline / primary CTA;
  stat chips demoted off gradient. Biggest perceived-quality jump.
- **Surfaces:** calmed hover lift + ease-lux, lifted-glass elevation on hover, framed
  tile media + vignette, frosted badge, elevated testimonial quote, unified logo wall,
  tamed the perpetual breathing dot, transient `will-change`.
- **[DEFECT fixed] Signature was rendering in a BANNED OS serif** (Instrument Serif
  never loaded) — now Inter; fallbacks hardened to sans-serif.
- **Perf-luxe:** cursor rAF now parks when idle + pauses on hidden; progress rail moved
  to CSS `animation-timeline: scroll()` (deleted a JS scroll listener); coalesced scroll
  reads. Bundle ~8.3 KB gzip (budget 50).
- **Premium copy:** availability → selective/calm; pricing de-discounted (band as fact,
  call as "fit check" not "free"); benefits 4→3; by-referral line on Contact.
- **Accessibility (owner-emphasized) — verified by Yuki, gaps fixed:** frosted badge
  now has an opaque theme-matched scrim (≥5.7:1 over any image, was ~1.1:1); light-theme
  CTA border raised to ≥3:1; testimonial weight 300→400; misleading a11y comments fixed.
  **New `TokenContrastTests` (8) permanently guard palette contrast (AA) for both themes.**

**Luxe follow-ups (deferred):** avatar 134 KB JPEG → AVIF/WebP `<picture>` at display size
(biggest CWV win, needs image tooling); signature container `aspect-ratio` should consume
JS `fieldAspect`; optional Home↔CV View-Transition morph.

**Remaining follow-ups (minor):** memoize `CultureService.CurrentCulture`; wire
`StoreSubscriber` into Masthead/CultureSelector/CookieBanner to retire per-service
events; EN training category still shows "IA" (touches OrderedCategories code); bump
outdated NuGet/npm minors in a dedicated PR; `sudo rm -rf netlify/functions/
node_modules.root-old`. AOT-drop / ICU-trim still want a measured publish-size spike.

